workflows:
  ios-workflow:
    name: iOS Workflow
    max_build_duration: 120

    environment:
      xcode: 16.4
      ios_signing:
        distribution_type: app_store
        bundle_identifier: com.magicfruitcasino.game
      groups:
        - MagicFruit

    scripts:
      - name: Sanity check env
        script: |
          set -e
          test -n "$APP_STORE_CONNECT_ISSUER_ID" || (echo "ISSUER_ID empty" && exit 1)
          test -n "$APP_STORE_CONNECT_KEY_ID" || (echo "KEY_ID empty" && exit 1)
          test -n "$APP_STORE_CONNECT_PRIVATE_KEY" || (echo "PRIVATE_KEY empty" && exit 1)

      - name: Fetch signing files
        script: |
          set -e
          app-store-connect fetch-signing-files \
            "com.magicfruitcasino.game" \
            --type IOS_APP_STORE \
            --issuer-id "$APP_STORE_CONNECT_ISSUER_ID" \
            --key-id "$APP_STORE_CONNECT_KEY_ID" \
            --private-key "$APP_STORE_CONNECT_PRIVATE_KEY" \
            --create

      - name: Setup keychain
        script: |
          set -e
          keychain initialize
          keychain add-certificates

      - name: Install CocoaPods
        script: |
          set -e
          pod install --repo-update

      - name: Apply provisioning to project
        script: |
          set -e
          xcode-project use-profiles --project "Unity-iPhone.xcodeproj"

      - name: Build iOS ipa
        script: |
          set -e
          xcode-project build-ipa \
            --workspace "Unity-iPhone.xcworkspace" \
            --scheme "MagicFruit" \
            --config Release

    artifacts:
      - build/ios/ipa/*.ipa
      - build/ios/logs/*.log
      - $HOME/Library/Logs/xcodebuild/*.log

    publishing:
      app_store_connect:
        issuer_id: $APP_STORE_CONNECT_ISSUER_ID
        key_id: $APP_STORE_CONNECT_KEY_ID
        api_key: $APP_STORE_CONNECT_PRIVATE_KEY
        submit_to_testflight: true
        submit_to_app_store: false